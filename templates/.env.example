# Copy to `.env` and fill in real values. The real `.env` is gitignored — NEVER commit it.
# Keep the keys here in sync with the env schema (see env.ts). Values here are placeholders only.

NODE_ENV=development
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# External services — leave blank here; set real values in your local .env only.
# API_KEY=
